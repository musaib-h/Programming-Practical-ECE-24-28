#include <stdio.h>
#include <math.h>

int main() {
    double num, square, root;
    printf("Enter a number: ");
    scanf("%lf", &num);
    square = pow(num, 2);
    root = sqrt(num);
    printf("Square: %.2lf\nSquare root: %.2lf\n", square, root);
    return 0;
}
